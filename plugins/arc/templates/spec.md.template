<!-- arc:spec -->
# Spec: Feature Title

## Context（背景・Why）

（なぜこの機能が必要か、どんな課題を解決するか）

## Goal

（達成したいアウトカム）

## Non-Goals

（明示的にスコープ外のこと）

## ADR（Architecture Decision Record）

### 採用したアプローチ

（選択したアーキテクチャ・実装方針）
- 使用するライブラリ・フレームワーク・パターン

### 検討した代替案と却下理由

- **案A**: [案の概要]
  - 却下理由: [技術的制約・コスト・複雑性・既存コードとの相性等]

- **案B**: [案の概要]
  - 却下理由: [...]

### トレードオフ・リスク・懸念点

- 設計による制約: [...]
- 既知のリスクと軽減策: [...]
- 将来への影響: [...]

## Feasibility Investigation

（/arc-investigating 実行後にIssueコメントとして別途投稿される）
