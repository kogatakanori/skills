<!-- arc:spec -->
# Spec: Feature Title

## Context（背景・Why）

（なぜこの機能が必要か、どんな課題を解決するか）

## Goal

（達成したいアウトカム。各Goalは「〜できる」「〜になる」という形式で記述する）

## Non-Goals

（明示的にスコープ外のこと。なぜスコープ外なのかも記述すると尚良い）

## Acceptance Criteria

各Goalに対するテスト可能な受け入れ基準。「〜した場合、〜となること」「〜できること」の形式で記述する。

- **AC-1**: （Goal 1に対応。例：「ユーザーが〜を入力した場合、〜が返されること」）
- **AC-2**: （Goal 2に対応）

## ADR（Architecture Decision Record）

※ このセクションは「意図するアプローチ」を記述する。実現性の確認は /arc-investigating が担う。
※ arc-investigating の結果によりアプローチが変わる場合、Goal/AC/Why は変えずこのADRのみ更新する。

### 採用したいアプローチ（意図）

（実現したい設計・実装方針）
- 使いたいライブラリ・フレームワーク・パターン（具体的な名前を明記）

### 検討した代替案と採用しない理由

- **案A**: [案の概要]
  - 採用しない理由: [設計思想・複雑性・一貫性等、意図に基づく理由]

- **案B**: [案の概要]
  - 採用しない理由: [...]

### 受け入れられるトレードオフ・許容できないリスク

- 受け入れるトレードオフ: [...]
- 許容できないリスク（これが発生したら方針を変える）: [...]
- 将来への影響: [...]

## Feasibility Investigation

（/arc-investigating 実行後にIssueコメントとして別途投稿される）
