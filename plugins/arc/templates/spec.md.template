<!-- arc:spec -->
# Spec: Feature Title

## Context（Why）

（なぜこれが必要か。解決する課題・痛み・背景）

## Goal（What）

（達成したいアウトカム。「〜できる」「〜になる」形式で記述）

## Acceptance Criteria

各Goalに対するテスト可能な受け入れ基準。「〜した場合、〜となること」の形式。

- **AC-1**: （Goal 1に対応）
- **AC-2**: （Goal 2に対応）

## Scope

**In Scope（今回やること）**
- ...

**Out of Scope（今回やらないこと・理由）**
- ...（なぜスコープ外かを明記する）

## Constraints（制約）

これらはAIが実装時に従うガードレール。違反した場合はCRITICALとして扱われる。

- **[制約の種類]**: [具体的な制約内容]
  - 例: `既存のAuth APIを使用すること。独自認証の実装は禁止`
  - 例: `API v1との後方互換性を維持すること`
  - 例: `ユーザーデータのスキーマ変更は禁止`

（制約がない場合は「なし」と記載）

---

※ ADR（アーキテクチャ決定記録）は /arc-investigating フェーズで作成される
