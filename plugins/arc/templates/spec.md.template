<!-- arc:spec -->
# Spec: Feature Title

## Context（Why）

（なぜこれが必要か。解決する課題・痛み・背景）

## Goal（What）

（達成したいアウトカム。「〜できる」「〜になる」形式で記述）

## Acceptance Criteria

各Goalに対するテスト可能な受け入れ基準。「〜した場合、〜となること」の形式。

- **AC-1**: （Goal 1に対応）
- **AC-2**: （Goal 2に対応）

## Scope

**In Scope（今回やること）**
- ...

**Out of Scope（今回やらないこと・理由）**
- ...（なぜスコープ外かを明記する）

## Constraints（ビジネス制約・不変条件）

実装方法に関係なく守らなければならないビジネスルール・ドメインの不変条件。
「何がNGか」をビジネス・ユーザー視点で記述する。実装方法（HOW）には踏み込まない。

- **[制約の種類]**: [具体的な制約内容]
  - 例: `既存ユーザーのデータが失われてはならない`
  - 例: `既存クライアントアプリへの後方互換性を壊してはならない`
  - 例: `GDPR準拠を維持すること（個人データの扱いを変えてはならない）`
  - 例: `現在の認証フローをユーザーに見せずに変更してはならない`

（ビジネス制約がない場合は「なし」と記載）

---

※ ADR（「どう実現するか」の技術選択）は /arc-investigating フェーズで作成される
