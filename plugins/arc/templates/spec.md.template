<!-- arc:spec -->
# Spec: Feature Title

## Context（Why）

（なぜこれが必要か。解決する課題・痛み・背景）

## Goal（What）

（達成したいアウトカム。「〜できる」「〜になる」形式で記述）

## Acceptance Criteria

各Goalに対するビジネス視点での完了条件。「〜した場合、〜となること」の形式。

- **AC-1**: （Goal 1に対応）
- **AC-2**: （Goal 2に対応）

## Constraints（ビジネス制約・不変条件）

実装方法に関係なく守らなければならないビジネスルール・ドメインの不変条件。
「何がNGか」をビジネス・ユーザー視点で記述する。実装方法（HOW）には踏み込まない。

- **[制約の種類]**: [具体的な制約内容]
  - 例: `既存ユーザーのデータが失われてはならない`
  - 例: `既存クライアントアプリへの後方互換性を壊してはならない`
  - 例: `GDPR準拠を維持すること（個人データの扱いを変えてはならない）`
  - 例: `月額予算○○円を超えてはならない`

（ビジネス制約がない場合は「なし」と記載）

## Domain Model（ことばの定義）

この機能で登場するエンティティ・概念の定義。チーム全員が同じ言葉で話せるようにする。

- **[用語]**: [定義]
  - 例: `ユーザー`: ログイン済みのアカウント保持者
  - 例: `ゲスト`: 未ログインのアクセス者
  - 例: `注文`: カートに商品を入れ、決済を完了した取引

（独自の用語・概念がない場合は「なし」と記載）

---

※ スコープ・実装アプローチ・技術選択（ADR）は /arc-investigating フェーズで決定する
